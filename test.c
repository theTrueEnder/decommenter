int score = 0;
int total_questions = 5;

// NEWLINE Loop through questions and update score accordingly
while (current_question < total_questions) {
    /* BLOCK this is a normal block comment   */
    string s = "//"; // INLINE /* and this
    // NEWLINE is a
    // NEWLINE multiline
    // NEWLINE block comment
    // NEWLINE commented out */
    int score = 0;
    s = '//'; // INLINE
    s += '//';//INLINE
    
    // NEWLINE Evaluate user's answer and update score
    if (answer_is_correct) { // INLINE other tricky situation *//*
        score++;
    }
     
    // NEWLINE Present the next question and repeat the process
    printf("Score: %d/%d\n", score, total_questions);
    // NEWLINE Display feedback and next question
    printf("Next question:\n"); 
}

/*/* BLOCK this is
a block comment /**/
// NEWLINE with tricky things  */
  // NEWLINE comment 2
  // NEWLINE comment 1
